dependencies:
  idf: ">=5.0"
  espressif/usb_host_uvc: ^2.2.0
  espressif/esp_new_jpeg: ^1.0.1
  espressif/esp_painter: ^0.1.0
  espressif/button: ^4.1

  espressif/esp32_p4_function_ev_board:
    version: ^5.2.3
    rules:
      - if: "target in [esp32p4]"
  espressif/esp32_s3_lcd_ev_board:
    version: "^4.0.0"
    rules:
      - if: "target in [esp32s3]"
