dependencies:
  idf: ">=5.0"
  espressif/usb_host_uac: ^1.5.0
  espressif/usb_host_uvc: "^2.5.1"
  espressif/cjson: "^1.7"

  espressif/esp_wifi_remote:
    version: ">=0.10,<2.0"
    rules:
      - if: "target in [esp32p4]"
  espressif/esp_hosted:
    version: "2.12.3"
    rules:
      - if: "target in [esp32p4]"

  captive_dns:
    version: "*"
    override_path: ../../../../common_components/captive_dns
