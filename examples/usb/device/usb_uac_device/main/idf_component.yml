## IDF Component Manager Manifest File
dependencies:
  cmake_utilities: "*"
  espressif/tinyusb:
    version: ">=0.15.0~3"
    rules:
      - if: "idf_version >=5.0"
  leeebo/tinyusb_src:
    version: ">=0.15.0~6"
    rules:
      - if: "idf_version >=4.4,<5.0"
