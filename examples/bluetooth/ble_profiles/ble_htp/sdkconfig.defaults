# Override some defaults so BT stack is enabled
# by default in this example
CONFIG_BT_ENABLED=y
CONFIG_BT_NIMBLE_ENABLED=y

CONFIG_BLE_CONN_MGR_ROLE_CENTRAL=y
CONFIG_BLE_HEALTH_THERMOMETER_PROFILES=y
